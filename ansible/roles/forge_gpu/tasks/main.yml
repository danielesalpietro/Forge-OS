---
# forge_gpu: NVIDIA drivers (Canonical-signed modules), container toolkit and GPU operator prerequisites
# Status: Phase 3 placeholder. See docs/PLAN.md for the deliverables and
# acceptance criteria; implementation lands in the Phase 3 milestone.
- name: "Placeholder for forge_gpu (Phase 3, not implemented yet)"
  ansible.builtin.debug:
    msg: "NVIDIA drivers (Canonical-signed modules), container toolkit and GPU operator prerequisites - not implemented yet (Phase 3); see docs/PLAN.md"
